@model IEnumerable<Arac_Kirala.Models.Modeller>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModellerAdi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModellerAdi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModellerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModellerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModellerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
